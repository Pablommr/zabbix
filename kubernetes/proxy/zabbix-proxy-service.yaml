apiVersion: v1
kind: Service
metadata:
  name: zabbix-proxy-service
  namespace: monitoring
spec:
  ports:
  - port: 10051
    targetPort: zabbix-port
    protocol: TCP
    name: zabbix-proxy
  selector:
    app: zabbix-proxy